root: NetIdMobileSdk
fastlane:
    xcode_version: 13
    project: NetIdMobileSdk.xcodeproj
    build:
      - 
        branch: build
        scheme: NetIdMobileSdk-App
        target: NetIdMobileSdk-App
        test_scheme: NetIdMobileSdk
        plist: NetIdMobileSdk-App/Info.plist
        keychain: netid-appstore
        export_method: app-store
        configuration: Release
    release:
      -
        branch: build
        scheme: NetIdMobileSdk-App
        target: NetIdMobileSdk-App
        test_scheme: NetIdMobileSdk
        plist: NetIdMobileSdk-App/Info.plist
        keychain: netid-appstore
        export_method: app-store
        testflight:
          team_id: 8G463Z6GPS

